<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Contenedor que incluye tanto el texto como la flecha -->
    <FrameLayout
        android:id="@+id/container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:background="@drawable/bg_rounded_gray_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Pregunta frecuente -->
        <TextView
            android:id="@+id/preguntafrecuentedisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="40dp"
            android:textSize="15sp"
            android:textColor="@color/black"
            tools:text="¿Qué hago si me quedo rezagado o tengo un problema mecánico"/>

        <!-- Flecha dentro del contenedor -->
        <ImageButton
            android:id="@+id/arrow1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:src="@drawable/ic_next"
            android:background="@null"
            android:layout_marginEnd="12dp"/>
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
